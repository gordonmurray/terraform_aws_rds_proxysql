[defaults]
# Fresh EC2 instances won't be in known_hosts yet.
host_key_checking = False
# Silence the interpreter-discovery warning; the targets are Python 3 only.
interpreter_python = auto_silent
retry_files_enabled = False
