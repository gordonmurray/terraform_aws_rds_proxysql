---
# Maps the names the playbooks/roles use to the secret values in vault.yml.
# Keep the actual passwords out of here — they live (encrypted) in vault.yml.
proxysql_admin_password: "{{ vault_proxysql_admin_password }}"
proxysql_monitor_password: "{{ vault_monitor_user_password }}"
app_user_password: "{{ vault_app_user_password }}"
monitor_user_password: "{{ vault_monitor_user_password }}"
